Help me produce a finished draft for a post. Work in this order:

1. Read the folder's resources. Infer the **format** — shortform (e.g. a tweet / X post) or longform (e.g. a blog post). If the resources make it clear (a note states the format, or it's obvious from context), assume it and tell me which signal you used. Otherwise ask me.
2. In the same way, infer the **topic / goal** of the post from the resources. Only ask me if you genuinely can't conclude.
3. Propose a full draft inline in the chat — not a skeleton, not bullets, the real thing.
4. Ask whether I'd like you to save it as a file in this folder (suggest a filename) or whether I want changes. Iterate with me until I say save — then write the file.

Do not assume a format or topic when the evidence is ambiguous. Asking is better than guessing wrong.
